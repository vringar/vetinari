//! Insulation layer between the orchestrator and crosslink's Rust crates (REQ-3a).
//!
//! Real adapter surface lands in crosslink issue #3 (F3). Until then this crate
//! exposes only the crate identity so the workspace links.
