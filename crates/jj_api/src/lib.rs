//! Insulation layer between the orchestrator and jj-lib (REQ-1c).
//!
//! Real adapter surface lands in crosslink issue #4 (F4). Until then this crate
//! exposes only the crate identity so the workspace links.
